name: ihunt
services:
  api:
    build:
      dockerfile: "./config/Dockerfile"
      context: .
      platforms:
        - linux/amd64
    image: ghcr.io/chaosexanima/ihunt:latest
    restart: unless-stopped
    volumes:
      - ./config/data/uploads:/media
    env_file: .env.production
    environment:
      UPLOAD_PATH: /media
      MEDIA_PATH: /media
      PUBLIC_HOST: https://ihunt-eu.com
      LAN_HOST: https://lan.ihunt-eu.com

  images:
    image: ghcr.io/imgproxy/imgproxy:latest
    restart: unless-stopped
    volumes:
      - ./config/data/uploads:/mnt/images:ro
    env_file: .env.production
    environment:
      PORT: 9000
      IMGPROXY_WORKERS: 20
      IMGPROXY_USE_ETAG: "true"
      IMGPROXY_LOCAL_FILESYSTEM_ROOT: "/mnt/images"
      IMGPROXY_PATH_PREFIX: /images

  static:
    image: nginx
    restart: unless-stopped
    volumes:
      - ./config/nginx.conf:/etc/nginx/templates/default.conf.template:ro
      - ./dist/client:/etc/nginx/html:ro
      - nginx_cache:/var/cache/nginx:delegated
    links:
      - images:images
      - api:api
    ports:
      - "9999:80"

volumes:
  nginx_cache:
